#pragma once

#include "mat_type.h"

namespace csml
{
// 	class AFX_EXT_CLASS mat_factory
// 	{
// 	public:
// 		static cuFloat::Ptr alloc();
// 		static cuFloat::Ptr alloc(archive & ar);
// 		static cuFloat::Ptr alloc(cuFloat::Ptr & lhs);
// 		static cuFloat::Ptr alloc(size_t n, size_t c);
// 		static cuFloat::Ptr alloc(size_t n, size_t c, size_t h, size_t w);
// //		static cuFloat::Ptr alloc(int n, int c, int h, int w, float * pData);
// 	};

}
